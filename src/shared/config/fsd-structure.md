# Структура проекта по методологии Feature-Sliced Design (FSD)

## Обзор слоев

Проект организован по методологии FSD, которая разделяет код на логические слои:

### 📱 `app/` - Инициализация приложения
- Конфигурация Next.js App Router
- Глобальные провайдеры
- Layout компоненты
- Глобальные стили

### 📄 `pages/` - Страницы приложения
- Композиция виджетов для конкретных страниц
- Каждая страница = композиция виджетов
- Пример: `pages/home/`, `pages/product/`

### 🧩 `widgets/` - Крупные самостоятельные блоки
- Крупные составные компоненты интерфейса
- Композиция features и entities
- Пример: `widgets/header/`, `widgets/product-list/`

### ⚡ `features/` - Функциональные возможности
- Конкретные пользовательские сценарии
- Интерактивные элементы
- Пример: `features/add-to-cart/`, `features/search/`

### 🏗️ `entities/` - Бизнес-сущности
- Бизнес-логика и данные
- Модели данных
- Пример: `entities/product/`, `entities/user/`

### 🔧 `shared/` - Переиспользуемый код
- `ui/` - UI компоненты (кнопки, инпуты и т.д.)
- `lib/` - Утилиты и вспомогательные функции
- `api/` - API клиенты и конфигурация
- `config/` - Конфигурация приложения
- `assets/` - Статические ресурсы

## Правила импорта

Слои могут импортировать только из нижележащих слоев:

```
app → pages → widgets → features → entities → shared
```

**Запрещено:**
- ❌ `entities` импортирует из `features`
- ❌ `shared` импортирует из `entities`
- ❌ Импорт из того же слоя (кроме внутренних модулей)

**Разрешено:**
- ✅ `features` импортирует из `entities` и `shared`
- ✅ `widgets` импортирует из `features`, `entities` и `shared`
- ✅ `pages` импортирует из всех нижележащих слоев

## Примеры использования

### Создание новой фичи
```
src/features/add-to-cart/
  ├── ui/
  │   └── AddToCartButton.tsx
  ├── model/
  │   └── useAddToCart.ts
  └── index.ts
```

### Создание новой сущности
```
src/entities/product/
  ├── ui/
  │   └── ProductCard.tsx
  ├── model/
  │   └── types.ts
  ├── api/
  │   └── productApi.ts
  └── index.ts
```

### Создание виджета
```
src/widgets/product-list/
  ├── ui/
  │   └── ProductList.tsx
  └── index.ts
```

## Алиасы путей

В `tsconfig.json` настроены следующие алиасы:
- `@/app/*` → `src/app/*`
- `@/pages/*` → `src/pages/*`
- `@/widgets/*` → `src/widgets/*`
- `@/features/*` → `src/features/*`
- `@/entities/*` → `src/entities/*`
- `@/shared/*` → `src/shared/*`

Пример использования:
```typescript
import { Button } from '@/shared/ui';
import { ProductCard } from '@/entities/product';
import { AddToCart } from '@/features/add-to-cart';
```

